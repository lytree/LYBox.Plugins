<UserControl xmlns="https://github.com/avaloniaui"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:vm="clr-namespace:Avalonia.Plugin.TDLSharp.ViewModels"
			 xmlns:models="clr-namespace:Avalonia.Plugin.TDLSharp.Models"
			 xmlns:controls="clr-namespace:Avalonia.Plugin.TDLSharp.Controls"
			 x:Class="Avalonia.Plugin.TDLSharp.Pages.DeepCopyPage"
			 x:DataType="vm:DeepCopyViewModel"
			 mc:Ignorable="d" d:DesignWidth="900" d:DesignHeight="900">
	<ScrollViewer>
		<StackPanel Margin="16" Spacing="12">

			<StackPanel Spacing="4" Margin="0 0 0 4">
				<TextBlock Text="{Binding Script.Name}" Theme="{StaticResource TitleTextBlock}" Classes="H3" />
				<TextBlock Text="{Binding Script.Description}" Classes="Secondary" />
			</StackPanel>

			<Border Theme="{DynamicResource CardBorder}" Padding="16">
				<StackPanel Spacing="12">
					<TextBlock Text="参数配置" Classes="SectionTitle" />
					<ItemsControl ItemsSource="{Binding Parameters}">
						<ItemsControl.DataTemplates>
							<DataTemplate x:DataType="models:TextScriptParameter">
								<Grid ColumnDefinitions="160,*" Margin="0 4" MinHeight="32">
									<TextBlock Grid.Column="0" Text="{Binding DisplayName}" VerticalAlignment="Center" Classes="Body" />
									<TextBox Grid.Column="1" Text="{Binding DefaultValue}" PlaceholderText="{Binding Description}" HorizontalAlignment="Stretch" />
								</Grid>
							</DataTemplate>
							<DataTemplate x:DataType="models:MultiLineTextScriptParameter">
								<Grid ColumnDefinitions="160,*" Margin="0 4">
									<TextBlock Grid.Column="0" Text="{Binding DisplayName}" VerticalAlignment="Top" Classes="Body" Margin="0 6 0 0" />
									<TextBox Grid.Column="1" Text="{Binding DefaultValue}" PlaceholderText="{Binding Description}" HorizontalAlignment="Stretch" AcceptsReturn="True" TextWrapping="Wrap" Height="120" />
								</Grid>
							</DataTemplate>
							<DataTemplate x:DataType="models:BoolScriptParameter">
								<Grid ColumnDefinitions="160,*" Margin="0 4" MinHeight="32">
									<TextBlock Grid.Column="0" Text="{Binding DisplayName}" VerticalAlignment="Center" Classes="Body" />
									<ToggleSwitch Grid.Column="1" IsChecked="{Binding DefaultBoolValue}" OnContent="是" OffContent="否" />
								</Grid>
							</DataTemplate>
							<DataTemplate x:DataType="models:NumberScriptParameter">
								<Grid ColumnDefinitions="160,*" Margin="0 4" MinHeight="32">
									<TextBlock Grid.Column="0" Text="{Binding DisplayName}" VerticalAlignment="Center" Classes="Body" />
									<NumericUpDown Grid.Column="1" Value="{Binding DefaultIntValue}" Minimum="0" HorizontalAlignment="Stretch" />
								</Grid>
							</DataTemplate>
							<DataTemplate x:DataType="models:PathScriptParameter">
								<Grid ColumnDefinitions="160,*,160" Margin="0 4" MinHeight="32">
									<TextBlock Grid.Column="0" Text="{Binding DisplayName}" VerticalAlignment="Center" Classes="Body" />
									<TextBox Grid.Column="1"
											 Text="{Binding DefaultValue}"
											 PlaceholderText="{Binding Description}"
											 HorizontalAlignment="Stretch"
											 IsReadOnly="True" />
									<Button Grid.Column="2"
											Content="浏览" Command="{Binding BrowseCommand}"
											VerticalAlignment="Center" Margin="8,0,0,0" />
								</Grid>
							</DataTemplate>
							<DataTemplate x:DataType="models:HistoryScriptParameter">
								<Grid ColumnDefinitions="160,*" Margin="0 4" MinHeight="32">
									<TextBlock Grid.Column="0" Text="{Binding DisplayName}" VerticalAlignment="Center" Classes="Body" />
									<controls:HistoryComboBox Grid.Column="1"
															  Text="{Binding DefaultValue, Mode=TwoWay}"
															  PlaceholderText="{Binding Description}"
															  HistoryKey="{Binding HistoryKey}"
															  HorizontalAlignment="Stretch" />
								</Grid>
							</DataTemplate>
						</ItemsControl.DataTemplates>
					</ItemsControl>

					<StackPanel Orientation="Horizontal" Spacing="12" Margin="0 8 0 0">
						<Button Content="执行" Command="{Binding ExecuteScriptCommand}" Classes="Primary" MinWidth="100" IsEnabled="{Binding !IsRunning}" />
						<Button Content="取消" Command="{Binding CancelExecutionCommand}" MinWidth="80" IsVisible="{Binding IsRunning}" />
						<TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" Classes="Tertiary" Margin="12 0 0 0" />
					</StackPanel>
				</StackPanel>
			</Border>

			<Border Theme="{DynamicResource CardBorder}" Padding="12" MaxHeight="{Binding LogMaxHeight}">
				<Grid RowDefinitions="Auto,*">
					<Grid ColumnDefinitions="*,Auto" Margin="0 0 0 8">
						<TextBlock Grid.Column="0" Text="日志输出" Classes="SectionTitle" VerticalAlignment="Center" />
						<Button Grid.Column="1" Content="清除" Command="{Binding ClearLogCommand}" FontSize="12" Padding="8 4" />
					</Grid>
					<ListBox x:Name="LogListBox" Grid.Row="1"
							 ItemsSource="{Binding LogEntries}"
							 SelectionMode="Toggle"
							 ScrollViewer.VerticalScrollBarVisibility="Auto"
							 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
						<ListBox.ItemsPanel>
							<ItemsPanelTemplate>
								<VirtualizingStackPanel />
							</ItemsPanelTemplate>
						</ListBox.ItemsPanel>
						<ListBox.Styles>
							<Style Selector="ListBoxItem">
								<Setter Property="Padding" Value="2 1" />
								<Setter Property="HorizontalContentAlignment" Value="Stretch" />
							</Style>
						</ListBox.Styles>
						<ListBox.ItemTemplate>
							<DataTemplate x:DataType="models:LogEntry">
								<DockPanel>
									<SelectableTextBlock Text="{Binding FormattedLine}" FontSize="12" FontFamily="Consolas" TextWrapping="Wrap" HorizontalAlignment="Stretch">
										<SelectableTextBlock.ContextMenu>
											<ContextMenu x:CompileBindings="False">
												<MenuItem Header="复制" Command="{Binding $parent[ListBox].DataContext.CopyLogEntryCommand}" CommandParameter="{Binding}" />
											</ContextMenu>
										</SelectableTextBlock.ContextMenu>
									</SelectableTextBlock>
								</DockPanel>
							</DataTemplate>
						</ListBox.ItemTemplate>
					</ListBox>
				</Grid>
			</Border>

			<Border Theme="{DynamicResource CardBorder}" Padding="12">
				<Grid RowDefinitions="Auto,*">
					<Grid ColumnDefinitions="*,Auto,Auto" Margin="0 0 0 8">
						<TextBlock Grid.Column="0" Text="执行历史" Classes="SectionTitle" VerticalAlignment="Center" />
						<Button Grid.Column="1" Content="应用" Command="{Binding ApplyHistorySourceCommand}" CommandParameter="{Binding SelectedHistoryRecord}" FontSize="12" Padding="8 4" Margin="0 0 8 0" IsEnabled="{Binding SelectedHistoryRecord, Converter={x:Static ObjectConverters.IsNotNull}}" />
						<Button Grid.Column="2" Content="清除全部" Command="{Binding ClearHistoryCommand}" FontSize="12" Padding="8 4" />
					</Grid>
					<DataGrid Grid.Row="1" ItemsSource="{Binding HistoryRecords}" SelectedItem="{Binding SelectedHistoryRecord}"
							  AutoGenerateColumns="False" IsReadOnly="True" CanUserResizeColumns="True"
							  MaxHeight="250" SelectionMode="Single">
						<DataGrid.Columns>
							<DataGridTextColumn Header="状态" Binding="{Binding StatusIcon}" Width="50" />
							<DataGridTextColumn Header="源频道" Binding="{Binding SourceChannel}" Width="*" />
							<DataGridTextColumn Header="执行时间" Binding="{Binding ExecutedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="140" />
							<DataGridTextColumn Header="结果" Binding="{Binding Status}" Width="70" />
							<DataGridTextColumn Header="错误信息" Binding="{Binding ErrorMessage}" Width="200" />
						</DataGrid.Columns>
						<DataGrid.ContextMenu>
							<ContextMenu x:CompileBindings="False">
								<MenuItem Header="应用到输入框" Command="{Binding ApplyHistorySourceCommand}" CommandParameter="{Binding SelectedHistoryRecord}" />
								<MenuItem Header="删除此记录" Command="{Binding DeleteHistoryRecordCommand}" CommandParameter="{Binding SelectedHistoryRecord}" />
							</ContextMenu>
						</DataGrid.ContextMenu>
					</DataGrid>
				</Grid>
			</Border>

		</StackPanel>
	</ScrollViewer>
</UserControl>
